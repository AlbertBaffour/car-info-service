spring.jpa.hibernate.ddl-auto=create-drop
spring.datasource.url=jdbc:mariadb://${MYSQL_DB_HOST:localhost}:3306/springbootdb?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true
spring.datasource.username=${MARIADB_USERNAME:root}
spring.datasource.password=${MARIADB_PASSWORD:admin}
spring.jpa.database-platform = org.hibernate.dialect.MariaDBDialect
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spirng.data.mariadb.port= ${MARIADB_PORT:3306}
#spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

server.port=8051